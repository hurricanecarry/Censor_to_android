# 操作手册：Unity 游戏 → Android 移植与缺陷定位

> 适用：Unity 2022.3 + IL2CPP +（YooAsset / AssetBundle）的 Windows 游戏，移植到 Android 真机并定位渲染类缺陷。
> 本文两部分：**① 完整流程**（照着走一遍）与 **② 命令与坑速查**（查表用）。
> 项目结论与数据见 `README.md`；本文引用的脚本都在 `tools/`。

---

## 第一部分 · 完整流程

以 **Censor v5.1.0** 为实例（Unity 2022.3 + AssetBundle/YooAsset + IL2CPP）
编写：hurricanecarry，2026-09-23。配套文档：本仓库 `docs/03-交付说明.md`、`docs/01-项目总结.md`

---

### 0. 总览：七步流水线

```
[1] 盘点原版产物        →  搞清"原版把什么放在哪里"（不猜，用工具读）
[2] AssetRipper 导出    →  bundle/player data 还原成 Unity 工程
[3] 工程可编译化        →  引用/类型/程序集/剥离配置
[4] 资源导入档案修复    →  .meta 层（纹理/sprite/材质/着色器/字体）
[5] 资源包重建          →  YooAsset 采集器 + 构建 bundle + 包索引 + 地址映射
[6] 出 APK              →  IL2CPP 打包（命名/账本/sha256）
[7] 装机与验证          →  设备端 sha256 + 日志 + 截图/录像 + 编辑器 A/B 对照
        ↑ 全程可选：[8] 诊断探针（诊断包 + 控制文件，秒级迭代）
```

**贯穿原则**：每一步的"完成"都必须由**可观察产物**判定（像素 / mtime / 包内数据 / 设备日志），
不能由"我发起了/我设置了"判定。

---

### 1. 工具链清单

| 工具 | 版本（本次） | 路径 | 用途 |
|---|---|---|---|
| **AssetRipper** | 2.0.0 | `<TOOLS_DIR>\AssetRipper\AssetRipper.GUI.Free.exe` | **唯一的桥**：读 Unity 构建产物（bundle/player data）→ 重建 Unity 工程 |
| **Unity Editor** | 2022.3.62f3c1 | `<UNITY_EDITOR>\2022.3.62f3c1\Editor\Unity.exe` | 工程编译、资源编辑、bundle 构建、APK 构建 |
| **Unity MCP**（mcp-for-unity） | — | 编辑器内包 | 编辑器自动化 + **运行时取证**（实例化 prefab、读 usedMaterials、顶点色、渲染到 PNG） |
| **UnityPy** | 1.25.3 | `<PY_DEPS>` | **独立第二解码器**：直接读 bundle/APK 的序列化数据（判据权威来源） |
| **ilspycmd** | 11.0.0 | `<DOTNET_TOOLS>\tools\ilspycmd.exe` | 反编译 DLL（对比原版/我方运行时行为、定位根因） |
| **Pillow + numpy** | — | Python 3.14 | 像素级比对、拼图、统计（Laplacian 等） |
| **adb** | platform-tools | `<ANDROID_SDK>\platform-tools\adb.exe` | 装机、设备端 sha256、日志、截图、input |
| **YooAsset Editor** | 2.x | 包 `com.tuyoogame.yooasset` | 采集器配置、bundle 构建、manifest |
| **ffmpeg**（可选） | — | — | 音视频转码（原版音频格式与 Android 兼容性） |
| PowerShell | 5.1 / 7 | — | 构建/装机/清理脚本化 |

> **为什么要两个解码器**：AssetRipper 与 UnityPy 是两条独立实现。用 AssetRipper 导出的东西，
> 若再用 AssetRipper 去读原版做对照，差异无法归因（同源）。所以**原版读数一律用 UnityPy**。

---

### 2. 原版构建产物的结构（先会读它）

```
Censor_v5.1.0\
  Censor.exe
  TC_Data\
    globalgamemanagers                  ← PlayerSettings / QualitySettings / GraphicsSettings / TagManager…
    globalgamemanagers.assets           ← 常驻资源：内置 shader、MonoScript、PreloadData
    level0                              ← 启动场景（含 Canvas/摄像机等）
    resources.assets (+.resS/.resource) ← Resources 与大数据流（音频/视频/字体）
    sharedassets0.assets (+.resource)   ← 主场景资源
    Managed\*.dll                       ← 托管程序集（Assembly-CSharp.dll 与第三方 DLL）
    StreamingAssets\dynamic_assets\
        RootPackage\  (63 bundle + PackageManifest_*.bytes / .hash / .version)
        DLCPackage\   ( 5 bundle + manifest)
    app.info / boot.config / RuntimeInitializeOnLoads.json / ScriptingAssemblies.json
```

| 位置 | 里面有什么 | 用什么读 | 读什么 |
|---|---|---|---|
| `globalgamemanagers` | 工程级设置 | UnityPy `read_typetree()` | `PlayerSettings.m_ActiveColorSpace`、`QualitySettings.m_CurrentQuality`、`GraphicsSettings.m_CustomRenderPipeline` |
| `globalgamemanagers.assets` | 常驻 shader 等 | UnityPy | Shader `m_ParsedForm.m_Name`（**构建产物里 shader 自身的 `m_Name` 是空的，名字在 ParsedForm 里**） |
| `resources.assets` | 音视频/字体 | UnityPy / AssetRipper | AudioClip、VideoClip、Font、TMP |
| `Managed/*.dll` | 游戏代码 | ilspycmd | 反编译为 C# 源码 |
| `StreamingAssets/dynamic_assets/*` | **资源包** | UnityPy | bundle 内资产；manifest 里的地址表（二进制，字符串是"长度前缀"存储） |

**manifest（YooAsset 二进制）解析技巧**：字符串以 `[长度字节][内容]` 存储；
当长度字节恰好是可打印字符时会粘到**前一个字符串尾部**，剥离条件是 **`ord(addr[-1]) == len(path)`**。

---

### 3. 资源类型 ⇄ 程序 ⇄ 工具 对照表（**核心**）

| # | 资源类型 | 原版位置 | 导出工具 | 工程内落点（AssetRipper 布局） | 编辑/修复工具 | 打包去向 | 校验方法 |
|---|---|---|---|---|---|---|---|
| 1 | **场景/预制体/GameObject/组件** | bundle | AssetRipper | `Assets/AssetBundles/<bundle>/GameObject|MonoBehaviour/` | Unity Editor（MCP 或脚本） | YooAsset bundle | 编辑器实例化 + 运行时字段 dump |
| 2 | **纹理 Texture2D** | bundle | AssetRipper | `.../Texture2D/*.png` + `.png.meta` | Unity **TextureImporter** | bundle / 玩家包 | UnityPy 读**包内 texel**（逐像素）+ 属性 |
| 3 | **Sprite** | bundle | AssetRipper | `.png.meta` 内的 sprite 表 | TextureImporter/`SpriteEditor` | bundle | 原版 `SpriteSettings` 位解码对照（`spriteMeshType`/pivot/textureRect） |
| 4 | **材质 Material** | bundle | AssetRipper | `.../Material/*.mat` | Unity Editor / 文本 | bundle | 与原版 `m_SavedProperties`+关键字+队列逐字段 |
| 5 | **Shader** | bundle/玩家包 | AssetRipper（**DummyShaderTextExporter → stub！**） | `.../Shader/*.shader` | **手工重建** | bundle（或玩家包，看落点） | 结构签名判 stub；属性数 + `RenderType/Queue` + `rtBlend*` 与原版编译态对照 |
| 6 | **Spine 数据** | bundle | AssetRipper | `TextAsset/*.atlas.txt|*.txt`、`MonoBehaviour/*_SkeletonData.asset|*_Atlas.asset`、`.../Texture2D/*.png` | Unity Editor + **Spine 官方运行时 DLL** | bundle | 运行时 dump（`SkeletonDataAsset.blendModeMaterials`、`AtlasPage.name`、`usedMaterials`） |
| 7 | **TextAsset（配置/本地化）** | bundle | AssetRipper | `.../TextAsset/` | 任意文本编辑 | bundle | 内容比对 + 运行时可读 |
| 8 | **ScriptableObject / 配置资产** | bundle | AssetRipper | `.../MonoBehaviour/*.asset` | Unity Editor | bundle | 逐字段与原版 typetree 对照 |
| 9 | **动画 AnimationClip / AnimatorController** | bundle | AssetRipper | `AnimationClip/`、`AnimatorController/` | Unity Editor | bundle | 运行时播放验证 |
| 10 | **音频 AudioClip** | `resources.assets` | AssetRipper（可能需重编码） | `.../AudioClip/` | ffmpeg/Unity | 玩家包 or bundle | 设备上实际能播（Android 解码器差异） |
| 11 | **视频 VideoClip** | `resources.assets(.resS)` | AssetRipper | `.../VideoClip/` | ffmpeg/Unity | 玩家包 | 设备硬件解码兼容（本项未系统回归） |
| 12 | **字体 Font / TMP** | `resources.assets` | AssetRipper | `.ttf`、`* SDF.asset`(MonoBehaviour) + atlas `.png` | Unity/TMP | 玩家包/bundle | 文字渲染；构建期 TMP 预处理器报错需单独查 |
| 13 | **程序集/代码** | `Managed/*.dll` | AssetRipper(+ILSpy 复核) | `Assets/Scripts/Assembly-CSharp/**`、`Assets/Plugins/*.dll` | 编辑器 + 文本编辑 | 玩家包（IL2CPP） | 编译通过 + **运行期行为判据**；剥离用 `link.xml` |
| 14 | **AssetBundle 本体 / manifest / 地址** | `StreamingAssets/dynamic_assets/*` | ❌**不能复用** | —— | **YooAsset 重建** | `Assets/StreamingAssets/dynamic_assets/<包>/` | 包内 container 地址集合与原版一致 |
| 15 | **工程设置** | `globalgamemanagers` | UnityPy 读 / 手工对齐 | `ProjectSettings/*.asset` | 文本或 Unity | 玩家包 | 从 APK 的 `globalgamemanagers` 读回比对 |
| 16 | **存档** | 不在包内（运行时路径） | — | 代码里改路径 | 代码 | — | Android 沙盒可写（`persistentDataPath`）+ 读档成功 |
| 17 | **输入（触摸/摇杆）** | 代码层 | — | `Assets/Scripts/.../Port/` | 代码 | — | 真机触摸可达性 |

#### 3.1 已知的 AssetRipper 导出"系统性 artefacts"（本项目实测，按危害排序）

1. **Shader 全部变 stub**（`DummyShaderTextExporter`）：属性表保留、代码被换成
   `return _MainTex.Sample(...) * _Color;` + `RenderType"="Opaque"` + **无 `Blend`**。
   ⇒ 症状：精灵以不透明绘制（NPC 隐形、立绘/描边画错、特效全失效）。
   ⇒ **识别靠结构签名**（Opaque + 无 Blend + dummy 标记），**不要**靠"注释里出现某个词"。
2. **PMA 图集的 `alphaIsTransparency: 1`**：Unity 导入器会把可见像素 RGB **"扩色"**进全透明区；
   PMA 加色/正片叠底会读这些 RGB ⇒ 块状杂色碎片。**修法：改成 0**（不变量：源 PNG 可以逐字节正确，
   但**导入后**的 texel 已变）。
3. **Sprite 几何**：`spriteMeshType`/pivot/`textureRect` 的换算偏差（Single 模式尤其）⇒ 立绘缺口。
4. **纹理 `sRGBTexture` 标志**：与原版可以不一致（但**必须逐资源核对**，本项目曾误判为根因）。
5. **每平台质量默认值**：`QualitySettings.m_PerPlatformDefaultQuality` 保留 Unity 默认
   （Android=2/Medium），而原版 Windows 跑 5/Ultra ⇒ 需对齐。
6. **扩展名大小写**：`.PNG` → `.png`（地址映射查表必须大小写不敏感）。
7. **每 bundle 目录内重复 shader/material 副本**（无共享抽取）⇒ 改 shader 要**改所有副本**；
   也意味着**包内 shader 与玩家包 shader 可能是两份**，改哪份要看材质引用的是哪个 GUID。
8. **`Resources/` 与 bundle 的落点差异**：`Assets/Resources/**` 进**玩家数据**（改它只需重出 APK），
   `Assets/AssetBundles/**` 进 **bundle**（改它必须重建 bundle）。

---

### 4. 端到端复刻步骤（照抄版）

#### Step 0 环境
- Unity **必须用与原版大版本一致**的编辑器（本工程 2022.3.x）；Android 模块 + IL2CPP + NDK 就绪；
- 装 AssetRipper / UnityPy / ilspycmd / adb；`adb devices` 能看到设备。

#### Step 1 导出工程
```
Start-Process '<TOOLS_DIR>\AssetRipper\AssetRipper.GUI.Free.exe' -ArgumentList @('--headless','--port','8090')
## 通过 http://127.0.0.1:8090 的 REST 提交导出任务：
##   输入 = 原版 TC_Data（含 StreamingAssets/dynamic_assets）
##   输出 = 新 Unity 工程（本次：<UNITY_PROJECT>）
```
坑：导出模式（Hybrid/Safe/Decompiled）会显著影响程序集与类型解析；先确认**资源没有静默丢失**
（用原版地址清单 vs 导出工程资源清单交叉核对，别信日志的"成功"）。

#### Step 2 工程可编译化
- 补齐 AssetRipper **故意不提供**的包（TMP / uGUI / Cinemachine / URP / Spine 运行时）——按原版实际用到的版本；
- 修反编译引入的语法/语义偏差（如 `?.Kill()` 被改写成别的形式）；
- `Assets/link.xml` 保留被 IL2CPP 剥离搞坏的反射目标（本次：`Newtonsoft.Json`）；
- 设置 Android 平台：IL2CPP、ARM64、minSdk、包名、版本号、debug keystore。

#### Step 3 资源导入档案（.meta）修复
- 纹理：`sRGBTexture`、`alphaIsTransparency`、`spriteMeshType`、平台格式覆盖、`mipmapEnabled`、filter/wrap/aniso；
- 用**原版读数**（UnityPy）做对照，逐项决定改不改；
- **改完要重新导入**（`AssetDatabase.ImportAsset(ForceUpdate)`），否则取到的是旧导入结果。

#### Step 4 Shader 真值化
- 扫全部 `.shader`，按结构签名找出 stub；
- 对每个 stub：先看**原版编译态**（UnityPy 读 `m_ParsedForm`：属性表 / `RenderType` / `Queue` /
  `rtBlend0/1` 的绑定名 / Stencil / ZWrite / Cull），据此重建**属性表完全一致、渲染状态一致**的实现；
- 被材质引用的副本**逐个都要改**；改完重建 bundle。

#### Step 5 资源包重建（YooAsset）
1. **采集器**（`Assets/AssetBundleCollectorSetting.asset`）：每个原版包一个 Package，每个原版 bundle 文件夹一个 collector；
2. **自定义规则**（本次）：
   - Pack：bundle 名 = `Assets/AssetBundles/<文件夹名>`（与原版 bundle 名一致）；
   - Address：查 `Assets/PortBuild/PortAddressMap.tsv`（`Address / OriginalAssetPath / ExportAssetPath / BundleName`），
     查不到就回退文件名。**地址必须来自原版 manifest**，不能从路径推导（游戏里是硬编码 key）；
3. **构建**：`BuiltinBuildPipeline`、`ForceRebuild`、LZ4、`HashName`、无加密、
   `BuildinFileCopyOption=ClearAndCopyAll` → 产出落到 `Assets/StreamingAssets/dynamic_assets/<包>/`；
4. **包索引**（`Assets/Resources/CensorPackageIndex.txt`）：声明每个包是否 `included` +
   manifest 文件名/大小/sha256/bundle 数/总字节。**运行时靠它决定是否初始化可选包**；
   注意：索引写坏会让玩家侧直接抛异常起不来 ⇒ 写入器要**共用**（本次抽了 `PortPackageIndex`）。

#### Step 6 出 APK
- `PortApkBuild`：自动编号 `bNN`（历史最大+1，扫磁盘+账本）、写 `BUILD_LEDGER.tsv`、打印 sha256；
- 发布包与诊断包用**编译宏**区分（`CENSOR_PORT_DIAGNOSTICS`），交付前必须确认包内**无 debug 宏**
  且 `dumpsys flags` 无 `DEBUGGABLE`。

#### Step 7 装机与验证
- 装机：`adb install -r`；**校验用设备端 `sha256sum`**（包大小无区分度）；
- 日志：`adb logcat -c` → **先挂 `adb logcat -s Unity > file` 再启动**（否则被系统/驱动刷屏挤掉）；
- 视觉：`adb shell screencap` + `adb pull`；与 Windows 参照做对照时必须**同状态、同帧**；
- 编辑器 A/B：同帧、同对象的单变量实验（改一个开关 → 渲染 → 逐像素比对）。

#### Step 8 诊断探针（成本极低、收益最大的一步）
- 一个**只在诊断包**编译的组件，读 `persistentDataPath/xxx_control.txt` 执行命令（dump 状态/切换开关/
  截图），输出到 `xxx_out.txt` 与 PNG；这样**一次装机可以跑几十个假设**；
- 纪律：控制文件用**随机 nonce**（内容相同只跑一次），只解析带当前 nonce 的段。

---

### 5. 判据：每种资源"怎么算修好了"

| 对象 | 判据（必须可观察） |
|---|---|
| 纹理 | ① 包内 texel 与参照逐像素；② 尺寸/格式/mip/滤波/包裹/aniso 不变；③ **透明像素 RGB**（PMA 图集）为 0 或大幅下降 |
| Shader | ① 属性数与属性名集合一致；② `RenderType`/`Queue`/`rtBlend*`/Stencil 与原版编译态一致；③ 构建日志无 shader error；④ 编辑器渲染正确 |
| 材质 | `m_SavedProperties`（Floats/Colors/TexEnvs/Ints）+ `m_ValidKeywords`/`m_InvalidKeywords` + `m_CustomRenderQueue` 逐字段 |
| 代码 | **行为**判据：设备 logcat / 功能可用，不能只看"编译通过" |
| 资源包 | manifest sha256、bundle 数、container 地址集合（与原版一致）、`included` 声明与真实文件一致 |
| 端到端 | 真机截图/录像 vs Windows 参照（同状态同帧）+ 用户实玩确认 |

---

### 6. 需要学的知识（按"不做就卡住 / 做了才顺 / 最值钱"分三档）

#### A. 必备（缺一项就推进不下去）
1. **Unity 资产模型**：`GUID` / `fileID` / `.meta` / YAML 序列化 / `Library` 缓存；
2. **AssetBundle 与 YooAsset（或 Addressables）**：bundle 名、manifest、**地址（address）**、采集器/打包规则、加载模式
   （OfflinePlayMode 从 StreamingAssets 读）；
3. **IL2CPP 与托管剥离**：AOT、`link.xml`、为什么反射代码会被剪掉（`MissingMethodException`/`ArgumentNullException`）；
4. **ShaderLab 渲染状态**：`Blend`（含 PMA `One OneMinusSrcAlpha`）、`ZWrite/ZTest`、`Cull`、`Stencil`、`Queue`；
   **预乘 alpha（PMA）vs 直通 alpha**、**sRGB 与色彩空间（Gamma/Linear）**；
5. **Android 打包基础**：Gradle、ARM64、minSdk/targetSdk、keystore、`adb`（install/logcat/screencap/input）。

#### B. 本项目实际用到的（进阶，决定效率）
6. **Spine 运行时**：SkeletonData/Atlas、`SkeletonGraphic`、`blendModeMaterials`、混合模式（Normal/Multiply/Additive/Screen）、
   PMA 顶点色技巧（additive 靠**顶点 alpha=0** 而非单独材质）；
7. **TextureImporter 全部关键项**：`sRGBTexture`、`alphaIsTransparency`、`spriteMeshType`、平台覆盖、压缩格式；
8. **URP/渲染管线设置**：质量分级、`m_CustomRenderPipeline`、2D Renderer、后处理；
9. **逆向读数**：AssetRipper（导出）、**UnityPy**（读 bundle/APK 的 typetree/container/texel）、**ILSpy**（读 DLL 行为）；
10. **像素分析**：Pillow/numpy（差分、Laplacian、直方图、拼图），把"看起来不对"变成数字；
11. **编辑器自动化**：Unity MCP / `-executeMethod` / `AssetDatabase` API（批量改导入设置、渲染到 RenderTexture）；
12. **脚本化构建**：PowerShell（注意：`&` 不等待 GUI 程序 → 用 `Start-Process -Wait`；
    `Start-Process -ArgumentList` 不给带空格路径加引号）；
13. **性能/生命周期**：Android 音频延迟、视频硬解、长时间运行内存增长（本项目未系统回归）。

#### C. 方法论（最值钱，比 API 知识重要）
14. **证据分级**：源文件一致 ≠ 导入后一致 ≠ 包内一致 ≠ 运行时一致；**判据要落在最靠近现象的那一层**；
15. **样本身份确认**：同名不同副本、跨帧/跨状态、取"第一个匹配"都会骗人；
16. **单变量 A/B/A**：一次只改一个东西，且用**同一帧、同一对象**验证；
17. **撤回纪律**：错了就写撤回记录（本项目撤回过 9 类错误，`108` 号有全表）——它是团队的资产；
18. **工具也要被验证**：审计脚本本身会骗人（我自己的审计脚本连续三次假警报）；
    涉及"名称匹配/大小写/反射取值"的地方最容易静默出错。

---

### 7. 一页速查

**常用命令**
```powershell
## AssetRipper 无头（REST 8090）
Start-Process '<TOOLS_DIR>\AssetRipper\AssetRipper.GUI.Free.exe' -ArgumentList @('--headless','--port','8090')
## Unity 三连（编辑器必须关闭；路径带空格要预加引号）
$u='<UNITY_EDITOR>\2022.3.62f3c1\Editor\Unity.exe'; $p='<工程路径>'
Start-Process $u -ArgumentList @('-projectPath',"`"$p`"",'-batchmode','-quit','-executeMethod','CensorPort.Editor.PortAndroidBuild.Run') -Wait -NoNewWindow
Start-Process $u -ArgumentList @('-projectPath',"`"$p`"",'-batchmode','-quit','-executeMethod','CensorPort.Editor.PortDlcBuild.Run')    -Wait -NoNewWindow
Start-Process $u -ArgumentList @('-projectPath',"`"$p`"",'-batchmode','-quit','-executeMethod','CensorPort.Editor.PortApkBuild.Run')    -Wait -NoNewWindow
## 装机 + 设备端校验
adb install -r <apk>; adb shell sha256sum (adb shell pm path <pkg> 去掉 package: 的路径)
## 边跑边抓日志（避免被系统刷屏挤掉）
adb logcat -c; adb logcat -s Unity > log.txt   # 另开一个终端再启动游戏
## 截图
adb shell screencap -p /sdcard/x.png; adb pull /sdcard/x.png x.png
```

**十个最常踩的坑**：① stub shader 以 Opaque 绘制 ② PMA 图集 `alphaIsTransparency=1` 扩色
③ 只重导材质没重导 shader ④ 改 shader 只改一份副本 ⑤ 地址从路径推导而非原版 manifest
⑥ 索引写坏导致玩家侧起不来 ⑦ 用包大小判断是否装对 ⑧ `logcat -d` 看不到 Unity 日志
⑨ 跨帧比较视觉 ⑩ 拿"编译通过"当作"修好了"

[本段写入完成] 2026-09-23 16:05 CST

---

## 第二部分 · 命令与坑速查

编写：hurricanecarry，2026-09-23。配套：本仓库 `docs/02-复刻手册_资源与工具对照表.md`、`docs/05-学习清单.md`

---

### 1. 构建（编辑器必须关闭；路径带空格要预加引号）

```powershell
$u = '<UNITY_EDITOR>\2022.3.62f3c1\Editor\Unity.exe'
$p = '<UNITY_PROJECT>'
$log = '<BUILD_DIR>\_probe\build.log'

function Invoke-Unity([string]$method, [string]$logPath) {
    Start-Process -FilePath $u -ArgumentList @(
        '-projectPath', "`"$p`"", '-batchmode', '-quit',
        '-executeMethod', $method, '-logFile', "`"$logPath`""
    ) -Wait -PassThru -NoNewWindow
}

Invoke-Unity 'CensorPort.Editor.PortAndroidBuild.Run' $log   # RootPackage bundle + 包索引
Invoke-Unity 'CensorPort.Editor.PortDlcBuild.Run'     $log   # DLCPackage bundle + 包索引
Invoke-Unity 'CensorPort.Editor.PortApkBuild.Run'     $log   # 非诊断 APK（P1-real-game）
## 诊断包（带探针，不要交付）: 'CensorPort.Editor.PortApkBuild.RunDiagnostic'
```
- 日志路径**必须预加引号**，否则 `Start-Process` 会按空格拆开（本项目曾把日志写到 `<BUILD_DIR>`）。
- `& Unity.exe ...`（call 运算符）**不会等待** GUI 程序 —— 必须 `Start-Process -Wait`。

### 2. 装机与设备端校验

```powershell
$adb = '<ANDROID_SDK>\platform-tools\adb.exe'
& $adb devices -l
& $adb install -r '<BUILD_DIR>\_probe\CensorPort_P1_b72.apk'
## 校验"装的确实是同一个包"：用设备端 sha256（包大小毫无区分度）
$path = (((& $adb shell pm path com.bigsstudio.thecensor.port) -join '') -replace '^package:', '').Trim()
(& $adb shell "sha256sum $path") -join ''          # 与本地 Get-FileHash 对比
& $adb shell dumpsys package com.bigsstudio.thecensor.port | Select-String 'flags=|versionName|lastUpdateTime'
## flags 里出现 DEBUGGABLE 说明是开发包，不能交付
```

### 3. 真机日志（**必须边跑边抓**）

```powershell
## 这台设备会刷出海量 gralloc 报错，logcat -d 会把 Unity 行挤掉 ⇒ 先挂过滤器再启动游戏
& $adb logcat -c
$proc = Start-Process $adb -ArgumentList @('logcat','-s','Unity') -RedirectStandardOutput log.txt -PassThru -NoNewWindow
& $adb shell am force-stop com.bigsstudio.thecensor.port
& $adb shell am start -n com.bigsstudio.thecensor.port/com.unity3d.player.UnityPlayerActivity
Start-Sleep -Seconds 90
Stop-Process -Id $proc.Id -Force
Select-String -Path log.txt -Pattern 'AssetsManager|DLCPackage|Exception|Shader error'
```

### 4. 截图与输入

```powershell
& $adb shell screencap -p /sdcard/x.png ; & $adb pull /sdcard/x.png x.png ; & $adb shell rm -f /sdcard/x.png
& $adb shell input tap 581 606        # 盲点导航（配合截图确认落点）
```

### 5. AssetRipper 无头导出

```powershell
Start-Process '<TOOLS_DIR>\AssetRipper\AssetRipper.GUI.Free.exe' -ArgumentList @('--headless','--port','8090')
## REST: http://127.0.0.1:8090  提交导出任务；导出模式(Hybrid/Safe/Decompiled)会影响程序集解析
```

### 6. UnityPy 常用片段（独立第二解码器）

```python
import sys; sys.path.insert(0, r'<PY_DEPS>')
import UnityPy

## (a) 读 bundle/APK 里的资产（Android 玩家数据是 GUID 命名的独立文件 + .splitN 分块）
env = UnityPy.load(bundle_path)
for o in env.objects:
    t = o.read_typetree(); print(o.type.name, t.get('m_Name'))

## (b) 按"资产路径"精确取资产（**不要按贴图名匹配，会重名串台**）
env = UnityPy.load(bundle_path)
for path, obj in env.container.items():        # path 是**全小写**的
    if path.endswith('cengceng.png') and obj.type.name == 'Texture2D':
        img = obj.read().image                  # PIL image
        tree = obj.read_typetree()              # 属性（尺寸/格式/mip 等）

## (c) 从 APK 里取玩家数据（globalgamemanagers 直接有；*.assets 是 .split0..N 分块，需拼接）
import zipfile
z = zipfile.ZipFile(apk); open('ggm.bin','wb').write(z.read('assets/bin/Data/globalgamemanagers'))
env = UnityPy.load('ggm.bin')
for o in env.objects:
    if o.type.name in ('PlayerSettings','QualitySettings','GraphicsSettings'):
        print(o.type.name, o.read_typetree())
```

### 7. Shader 编译态对照（判断 stub / 混合是否正确）

```python
## 构建后的 shader 自身 m_Name 为空，名字在 m_ParsedForm.m_Name
pf = o.read_typetree()['m_ParsedForm']
print(pf['m_Name'], len(pf['m_PropInfo']['m_Props']))                 # 属性数
sub = pf['m_SubShaders'][0]; print(sub['m_Tags'])                     # RenderType / Queue
st = sub['m_Passes'][0]['m_State']
print(st['rtBlend0'], st['rtBlend1'], st.get('zwrite'), st.get('cull'))  # 混合绑定名（如 _MySrcMode）
```

### 8. 反编译 DLL 对比运行时行为

```powershell
& '<DOTNET_TOOLS>\tools\ilspycmd.exe' -t Spine.Unity.SkeletonGraphic `
    '<UNITY_PROJECT>\Assets\Plugins\spine-unity.dll'
& '<DOTNET_TOOLS>\tools\ilspycmd.exe' -p -o out_dir <dll>     # 整个程序集
```

### 9. 像素差分（把"看起来不对"变成数字）

```python
import numpy as np
from PIL import Image
a = np.asarray(Image.open('before.png').convert('RGB'), dtype=np.int16)
b = np.asarray(Image.open('after.png').convert('RGB'), dtype=np.int16)
d = np.abs(a - b).max(axis=2)
print('differing px = %d (%.3f%%), mean = %.2f' % ((d > 6).sum(), 100.0*(d > 6).mean(), d.mean()))
## Laplacian 能量可量化"碎/噪"程度
```

---

### 10. 十个最常踩的坑（都吃过）

| # | 坑 | 症状 | 正确做法 |
|---|---|---|---|
| 1 | **Shader 是 AssetRipper stub**（属性全、代码假、`RenderType=Opaque`、无 `Blend`） | 精灵以不透明绘制；NPC 隐形、特效全失效 | 按**结构签名**识别；用原版编译态（属性表/Queue/`rtBlend*`）重建 |
| 2 | **PMA 图集 `alphaIsTransparency: 1`** 扩色 | 透明处出现块状杂色 | 改成 0；判据落在**包内 texel**（源 PNG 可能本来就正确） |
| 3 | 改了 shader 文件但只重导材质 | 读到的属性表还是旧的（假 MISSING） | 对 shader 本身 `ImportAsset(ForceUpdate)` 并核对 `GetPropertyCount()` |
| 4 | 只改了一份 shader 副本 | 改了没效果 | 每个 bundle 目录内都有副本；**材质引用哪份就改哪份** |
| 5 | 地址从路径推导 | 运行时报"找不到资产" | 地址来自**原版 manifest**（本项目 `PortAddressMap.tsv`） |
| 6 | 包索引写坏 | 玩家侧直接抛异常起不来 | 索引写入器**两包共用**，`included` 由"是否真有 manifest"决定 |
| 7 | 用包大小判断装对没 | 多个版本大小完全相同 | 用**设备端 `sha256sum`** |
| 8 | `logcat -d` 看不到 Unity 日志 | 以为游戏没输出 | 设备刷屏会挤掉环形缓冲 ⇒ **边跑边抓** |
| 9 | 跨帧/跨状态比较视觉 | 得出错误结论（本项最贵） | 同状态、同帧、同对象；先冻结再比 |
| 10 | "编译通过/我发起了"当作"修好了" | 假验收 | 判据绑定**可观察产物**（像素/包内数据/设备日志） |

[本段写入完成] 2026-09-23 16:45 CST
